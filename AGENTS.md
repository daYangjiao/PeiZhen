# Agent Instructions

- 如果有 `docs/` 文件夹，开始涉及代码、接口、页面或数据库的任务时，自动读取 `docs/` 下的全部文件，并按其中配置执行。
- 除非文件内容本身是命令或纯运行模板，修改代码、接口、页面、数据库或部署流程后，要同步更新 `docs/` 下相关文件的进度和说明。
- 真实密码、服务器连接、数据库连接、Token、私钥等本地机密只允许保存在本地忽略文件中，不得提交或推送。
- 修复或开发完成后，如果用户没有另行要求，提交并推送到 `FF`；推送到服务器源仓库后，服务器有 1 分钟轮询任务，会自动拉取 `FF` 并发布 backend、H5/admin 和 Android WGT。
- 推送后不要立即声称服务器已更新，必须等待轮询完成并检查服务器 release 元数据或服务状态。
